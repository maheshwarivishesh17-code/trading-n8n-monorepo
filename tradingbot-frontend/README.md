TradingBot frontend scaffold

Setup:

1. cd tradingbot-frontend
2. Run the installs per your preference (bun/npm/pnpm):
   - bun: `bun install`
   - npm: `npm install`
3. Start dev server: `bun run dev` or `npm run dev`

Dependencies recommended in package.json — adjust as needed.
